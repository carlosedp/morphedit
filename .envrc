# Nix flake development environment
# This file is used by direnv to automatically load the development environment
# when entering this directory.

use flake

# Optional: Load additional environment variables if needed
# export SOME_VAR=value

# Optional: Add local .env file if it exists
# dotenv_if_exists .env